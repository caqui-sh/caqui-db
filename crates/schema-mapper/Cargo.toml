[package]
name = "schema-mapper"
version = "0.0.6"
edition = "2024"

[dependencies]
schema-parser = { path = "../schema-parser" }
rusqlite = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }